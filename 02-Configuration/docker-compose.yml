services:
  n8n:
    # Version pinnee pour assurer la reproductibilite pedagogique.
    # Pour mettre a jour : testez d'abord avec n8nio/n8n:next avant de bumper la version.
    image: n8nio/n8n:1.70.0
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_EDITOR_BASE_URL=http://localhost:5678
      - N8N_PROTOCOL=http
      - GENERIC_TIMEZONE=Europe/Paris
    volumes:
      - n8n_data:/home/node/.n8n
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  n8n_data:
